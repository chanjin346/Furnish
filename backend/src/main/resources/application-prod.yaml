spring:
  datasource:
    url: jdbc:mariadb://prod-db-host:3306/furnish?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver
