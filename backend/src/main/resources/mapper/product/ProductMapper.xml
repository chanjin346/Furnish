<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.furnish.shop.product.mapper.ProductMapper">

    <resultMap id="ProductResultMap" type="com.furnish.shop.product.query.ProductQueryModel">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="price" property="price"/>
        <result column="stock" property="stock"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findById" parameterType="long" resultMap="ProductResultMap">
        SELECT id, name, price, stock, created_at
        FROM product
        WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="ProductResultMap">
        SELECT id, name, price, stock, created_at
        FROM product
        ORDER BY created_at DESC
    </select>

</mapper>